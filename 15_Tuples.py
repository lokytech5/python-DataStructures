# A tuples is a read only list that can't be modified
# The code sample below is an empty tuples
point = ()
# Here is a tuple without the brackets
points = 1,

# concatenating a tuples
numbers = (1, 2, 3) + (4, 5, 6)
print("tuple concatenate", numbers)

# Repeating a Tuples
num = (1, 2) * 3
print("Repeating a tuple", num)

# Converting list to a tuples
p = tuple([1, 2])
print("Converting list to tuples", p)